# Install everything with: brew bundle install --file=~/dotfiles/Brewfile

# Terminal
cask "wezterm"

# Prompt
brew "starship"

# Zsh plugins
brew "zsh-autosuggestions"
brew "zsh-syntax-highlighting"

# Fonts
cask "font-jetbrains-mono-nerd-font"

# Dotfiles symlink manager (used by install.sh if present)
brew "stow"
